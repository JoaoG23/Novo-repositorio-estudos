
        test('Se elemento está (Código da empresa) presente na tela', () => { 
            
            const idempresaEsperado = screen.getByText('Código da empresa')
            expect(idempresaEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (CNPJ) presente na tela', () => { 
            
            const cnpjEsperado = screen.getByText('CNPJ')
            expect(cnpjEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Nome fantasia) presente na tela', () => { 
            
            const nomeFantasiaEsperado = screen.getByText('Nome fantasia')
            expect(nomeFantasiaEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Razão social) presente na tela', () => { 
            
            const razaoSocialEsperado = screen.getByText('Razão social')
            expect(razaoSocialEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Telefone) presente na tela', () => { 
            
            const telefoneEsperado = screen.getByText('Telefone')
            expect(telefoneEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Fax) presente na tela', () => { 
            
            const faxEsperado = screen.getByText('Fax')
            expect(faxEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Email) presente na tela', () => { 
            
            const emailEsperado = screen.getByText('Email')
            expect(emailEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Site) presente na tela', () => { 
            
            const siteEsperado = screen.getByText('Site')
            expect(siteEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Endereço) presente na tela', () => { 
            
            const enderecoEsperado = screen.getByText('Endereço')
            expect(enderecoEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Número) presente na tela', () => { 
            
            const numeroEsperado = screen.getByText('Número')
            expect(numeroEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Complemento) presente na tela', () => { 
            
            const complementoEsperado = screen.getByText('Complemento')
            expect(complementoEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Bairro) presente na tela', () => { 
            
            const bairroEsperado = screen.getByText('Bairro')
            expect(bairroEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Cidade) presente na tela', () => { 
            
            const cidadeEsperado = screen.getByText('Cidade')
            expect(cidadeEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (CEP) presente na tela', () => { 
            
            const cepEsperado = screen.getByText('CEP')
            expect(cepEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Estado) presente na tela', () => { 
            
            const estadoEsperado = screen.getByText('Estado')
            expect(estadoEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Nome do responsável) presente na tela', () => { 
            
            const responsavelEsperado = screen.getByText('Nome do responsável')
            expect(responsavelEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (CPF Responsável) presente na tela', () => { 
            
            const cpfResponsavelEsperado = screen.getByText('CPF Responsável')
            expect(cpfResponsavelEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Ativo) presente na tela', () => { 
            
            const ativoEsperado = screen.getByText('Ativo')
            expect(ativoEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Inscrição estadual) presente na tela', () => { 
            
            const inscricaoestadualEsperado = screen.getByText('Inscrição estadual')
            expect(inscricaoestadualEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Celular) presente na tela', () => { 
            
            const celularEsperado = screen.getByText('Celular')
            expect(celularEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Servidor SMTP) presente na tela', () => { 
            
            const servidorSMTPEsperado = screen.getByText('Servidor SMTP')
            expect(servidorSMTPEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Porta SMTP) presente na tela', () => { 
            
            const portaSMTPEsperado = screen.getByText('Porta SMTP')
            expect(portaSMTPEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (E-mail From) presente na tela', () => { 
            
            const emailFROMEsperado = screen.getByText('E-mail From')
            expect(emailFROMEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Usuário/Email (Autenticação)) presente na tela', () => { 
            
            const usuarioEmailEsperado = screen.getByText('Usuário/Email (Autenticação)')
            expect(usuarioEmailEsperado).toBeInTheDocument();
            
        })
            
        test('Se elemento está (Senha (Autenticação)) presente na tela', () => { 
            
            const senhaEmailEsperado = screen.getByText('Senha (Autenticação)')
            expect(senhaEmailEsperado).toBeInTheDocument();
            
        })
            