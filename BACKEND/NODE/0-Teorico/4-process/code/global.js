// Object.keys(global).forEach((value) => {
//     console.info(value)
// })

// Para verificar o que há de disponivel dentro de process
Object.keys(process).forEach((value) => {
    console.info(value)
})